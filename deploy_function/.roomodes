customModes:
  - slug: interactive
    name: Interactive
    roleDefinition: CloudOps engeneer
    customInstructions: |-
      Создавай файлы в каталоге source.
      Для создания Cloud Function используй runtime Python 3.12.
      Не используй файл спецификации функции. Назови файл с кодом функции index.py, а вызываемую функцию - handler. Укажи корректную точку входа index.handler.
      Не забудь сформировать requirements.txt.
      Функция должна принимать через переменные окружения название бакета для очистки.
      Для работы с S3 используй  библиотеку boto3.
      Получи id сервисного аккаунта с именем 'sles-vibecoding' и используй его далее, если не указано иное.
      В качестве бакета для очистки используй 'sles-vibecoding', если не указан иной. Передавай бакет через переменные окружения версии функции.
      В качестве сервисного аккаунта для запуска функции из триггера используй аккаунт с именем 'sles-vibecoding', если не указан иной.
      Для развертывания в облаке используй YC CLI с активным профилем.
    groups:
      - read
      - edit
      - browser
      - command
      - mcp
    source: project
